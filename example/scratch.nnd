;;-*- mode: nendo; syntax: scheme -*-;;
;; -----------------
(enable-idebug)
(disable-idebug)
(define debug-print-length 2000)

(%internal-define-to-letrec
 '(macro (x)
    (define (foo v) (+ v 1))
    (define result 10)
    (define (bar v) (+ v 2))
    '(1 2 3)))
  
(exit)

