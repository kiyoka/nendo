;;-*- mode: nendo; syntax: scheme -*-;;

;; -----------------
(enable-idebug)
(disable-idebug)
(define debug-print-length 256)


(let loop ((arg1 nil)
           (arg2 nil))
  (if #t
      #t
      (loop 1 2)))

(define (foo a)
  (print a)
  (foo a+1))
(foo 1)


(macroexpand
 '(cond
   (#t
    1)
   (#t
    2)
   (else
    3)))


(if
 #t
 (begin 1)
 (if #t
     (begin 2)
     (if #t
         (begin 3)
         ())))

 
  
   
  

            
