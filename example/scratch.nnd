;;-*- mode: nendo; syntax: scheme -*-;;
;; -----------------
(enable-idebug)
(disable-idebug)
(define debug-print-length 2000)

(exit)


(define (looptest args)
  (define (loop count)
    (when (eq? 0 (% count 1000))
      (printf "count = %6d\n" count))
    (when (< count 10000)
      (loop count)))
  
  (print "abc"))

(pretty-print
 (disasm "looptest" 'expanded))

         

    



