;;-*- mode: nendo; syntax: scheme -*-;;
;; -----------------
(enable-idebug)
(disable-idebug)
(define debug-print-length 2000)

(define (foo x) x)
(export-to-ruby foo)

(define (clone x) x)
(export-to-ruby clone)

(define a 10)
(export-to-ruby a)

(exit)

