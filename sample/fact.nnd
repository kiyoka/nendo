#!/bin/sh
true; #-*- mode: nendo; syntax: scheme -*-;;
true; exec /usr/local/bin/nendo $0 $*

;; fact
(define (fact n)
  (if (zero? n)
      1
      (* n #?=(fact (- n 1)))))

(print (fact (to-i (second ARGV.to_list))))



